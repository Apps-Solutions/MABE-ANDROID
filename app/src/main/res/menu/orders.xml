<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/print_ticket"
        android:title="@string/receipt_print"
        app:showAsAction="never"
        android:visible="false">
    </item>

    <item
        android:id="@+id/save_changes"
        android:title="@string/receipt_send"
        app:showAsAction="always">
    </item>

    <!--
    <item android:id="@+id/save_changes"
        android:title="Guardar"
        android:icon="@drawable/ic_accept_enabled"
        app:showAsAction="ifRoom" />
    -->

    <item android:id="@+id/delete_product"
        android:title="Borrar producto"
        android:icon="@drawable/ic_delete_enabled"
        app:showAsAction="never"
        android:visible="false"/>

</menu>